    
    # else:
    #     if new_word in vocab[unit]:
    #         print(f"word {word} already exists in {unit}")
    #         exit()
    # vocab[unit].append(new_word)
    # save_vocab(vocab,filename)
    # for units in vocab:
    #     if unit in units and word not in unit:
    #         vocab[unit].append(new_word)
    #         save_vocab(vocab,filename)
# add_word()

#unit="unit_3",word="snow",meaning="שלג"


# def add_word(unit,word,meaning):
#     new_word={"word": word , "meaning" : meaning}
#     for units in vocab:
#         if unit in units:
#             if new_word["word"] not in unit:
#                 vocab[unit].append(new_word)
#         else:
#         try:
            
#             if new_word not in unit:


#     save_vocab(vocab,filename)

#def unit_exists(vocab, unit):
#     flag=True
#     if unit not in vocab:
#         flag=False
        
#     return flag
# unit_exists(vocab,unit)
